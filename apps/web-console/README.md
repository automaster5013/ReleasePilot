# Web Console

Next.js 기반 ReleasePilot 웹 콘솔입니다.

## 실행

```powershell
npm install
npm run dev
```

`http://localhost:3000`에서 확인합니다. `/health`는 컨테이너 probe용 JSON 응답을 제공합니다.

## 검사

```powershell
npm run lint
npm run typecheck
npm run build
```
