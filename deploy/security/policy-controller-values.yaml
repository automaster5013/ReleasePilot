webhook:
  # The low-cost demo runs one webhook replica. A production-like profile
  # must restore two replicas and required cross-zone anti-affinity.
  replicaCount: 1
  failurePolicy: Fail
  podDisruptionBudget:
    enabled: false
